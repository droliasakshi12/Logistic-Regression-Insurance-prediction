# 📊 Logistic Regression Insurance Prediction

A simple machine learning project that uses **Logistic Regression** to predict whether a person will buy insurance based on features from a dataset. This is a **binary classification** problem where the model predicts one of two possible outcomes (e.g., buy or not buy insurance). 

## 🧠 Project Summary
This project demonstrates how to:

- Load and explore an insurance dataset
- Preprocess data for modeling
- Train a Logistic Regression model
- Evaluate model performance
- Predict whether an individual will take insurance

Logistic Regression is a supervised learning algorithm used for binary classification tasks such as Yes/No or 0/1 predictions. It uses a sigmoid function to output probabilities and classify data points. 

---

### 🛠 Prerequisites

Install the required Python packages:

```bash
pip install pandas numpy scikit-learn matplotlib seaborn
```


## 📁 Repository Contents

| File/Folder | Description |
|-------------|-------------|
| `insurance_data.csv` | Dataset used for training and testing |
| `logistic_regression.ipynb` | Notebook with full implementation |
| `logistic_regression_insurance.ipynb` | Notebook focused on insurance prediction |
| `README.md` | Project documentation |

---

### 📈Expected Outcome 
- A trained Logistic Regression model that predicts insurance purchase behavior
- Evaluation metrics like accuracy and confusion matrix
- Visual insights into the model’s performance
